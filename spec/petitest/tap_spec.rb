require "spec_helper"

RSpec.describe Petitest::Tap do
  it "has a version number" do
    expect(Petitest::Tap::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
